var React = require('react');
var ReactDOM = require('react-dom');

(function () {
    'use strict';
    ReactDOM.render(
      <h1>Hello, World</h1>,
      document.getElementById('content')
    );

    console.log(2);
})();
